apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: treasure-frontend
  namespace: treasure
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - host: treasure.aitreasuremap.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: treasure-frontend
            port:
              number: 80
  - host: treasure.aitreasuremap.com
    http:
      paths:
      - path: /oauth2/idpresponse
        pathType: Prefix
        backend:
          service:
            name: treasure-frontend
            port:
              number: 80