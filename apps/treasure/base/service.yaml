apiVersion: v1
kind: Service
metadata:
  name: treasure-frontend
  namespace: treasure
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: treasure-frontend 