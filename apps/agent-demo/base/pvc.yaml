apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: langchain-data-pvc
  namespace: langchain
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: gp2 